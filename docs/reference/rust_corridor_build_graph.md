# Build a cached corridor graph from a raster grid

Build a cached corridor graph from a raster grid

## Usage

``` r
rust_corridor_build_graph(
  values,
  n_rows,
  n_cols,
  cell_width,
  cell_height,
  neighbours
)
```
